/**
 * Committed stub: `development` builds replace this file with `environment.dev.ts` (see angular.json).
 * Copy `environment.demo.ts` → `environment.dev.ts` before `npm start` or `npm run build:dev`.
 */
export { environment } from './environment.demo';
